import React from 'react';
import { SessionExpiredCard } from '../components/sessionExpired/SessionExpiredCard.tsx';

export const SessionExpiredPage: React.FC = () => {
  return <SessionExpiredCard />;
};
